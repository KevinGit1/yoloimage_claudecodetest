{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "command": "uv run ruff check --fix $CLAUDE_FILE_PATH",
        "description": "Auto-lint with ruff after every file edit"
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "command": "echo 'Running: $CLAUDE_BASH_COMMAND'",
        "description": "Log all shell commands before execution"
      }
    ]
  }
}
